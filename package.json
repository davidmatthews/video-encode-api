{
  "name": "video-encode-api",
  "version": "1.0.0",
  "description": "Video encoding job queue API for Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "cd worker && wrangler dev",
    "deploy": "cd worker && wrangler deploy",
    "db:create": "cd worker && wrangler d1 create video-encode-db",
    "db:migrate": "cd worker && wrangler d1 execute video-encode-db --file=../schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.78.0"
  },
  "dependencies": {
    "hono": "^4.3.0"
  }
}

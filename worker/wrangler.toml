name = "video-encode-api"
main = "src/index.ts"
compatibility_date = "2024-11-06"

# D1 Database binding
[[d1_databases]]
binding = "DB"
database_name = "video-encode-db"
database_id = "" # Will be set after creating the database

# Environment variables (API_KEY should be set as a secret)
# Use: wrangler secret put API_KEY
